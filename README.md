# Walmart-DataNexus
End-to-end Walmart data engineering platform that ingests data from SQL and AWS S3 using CDC and incremental pipelines, orchestrates workflows with Apache Airflow, transforms data in Databricks, performs quality checks, and enables SQL-based analytics through a chatbot.
